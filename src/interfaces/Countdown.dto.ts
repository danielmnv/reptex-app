interface CountdownDTO {
    days: number,
    hours: number,
    minutes: number,
    seconds: number,
    milliseconds: number,
    totalDays: number,
    totalHours: number,
    totalMinutes: number,
    totalSeconds: number,
    totalMilliseconds: number,

}

export default CountdownDTO;